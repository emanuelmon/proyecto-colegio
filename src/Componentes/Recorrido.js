import {Col, Container, Row } from "react-bootstrap"
import "react-multi-carousel/lib/styles.css"

export const Recorrido=()=>{
    return(
        <section className="recorrido" id="recorrido">
    </section>
    )
}